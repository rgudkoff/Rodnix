# IPC и порты

## Цели

- Сообщения фиксированной и переменной длины.
- Возможность передачи портов в сообщениях.
- Четкие контракты и валидация границ.

## Модель (MVP)

- Порты как именованные (или идентифицируемые) каналы.
- Сообщения:
  - fixed‑size (быстрый путь)
  - variable‑size (буфер + длина)
- Доставка: send/receive/peek (минимальный набор).

## Передача портов

- Порт может быть вложен в сообщение как capability‑объект.
- Валидность и право доступа проверяются на границе IPC.

## Инварианты

- Никакой передачи сырого указателя через IPC.
- Все длины и типы проверяются на входе.
- Все IPC‑сообщения имеют ABI‑заголовок (`abi_version`, `size`).

## Связь с IDL

- Форматы сообщений описываются в `.defs`.
- Генератор производит структуры и маршаллинг.

## Текущая реализация (v0)

- Есть базовые порты и очереди сообщений.
- Сообщения фиксированного размера (heap‑буфер до 4096 байт).
- `ipc_message_t` содержит ABI‑header и проверяется при отправке.
- Можно передавать порты в сообщениях (через массив port ids).
- Есть простое ожидание с таймаутом: polling + `scheduler_yield()`.
- Таймауты сейчас привязаны к scheduler‑тику (грубая точность).
- Есть базовое IPC‑наследование приоритетов в `ipc_send_receive()`/`ipc_receive()`/`port_set_receive()` (best‑effort, стек глубиной 4).
- Есть минимальная проверка прав (send/receive), но без per‑task namespace и полноценной модели прав.
- Нет peek и нет продвинутых политик очередей.

## Priority Inheritance (через IPC)

### Проблема

Если поток A (высокий приоритет) ждёт ответа от B (низкий приоритет),
возможна **priority inversion**.

### Решение

При блокировке A на IPC‑объекте:

1. Поток B временно получает:

```
dyn_priority_B = max(dyn_priority_B, dyn_priority_A)
```

2. Наследование фиксируется стеком (может быть вложенным).
3. После ответа B приоритет откатывается к предыдущему значению.

Это применяется **только на время IPC‑зависимости**.

## Где смотреть в коде

- Планируется в `kernel/common` и userland.
