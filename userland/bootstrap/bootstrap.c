/*
 * bootstrap.c
 * Skeleton for bootstrap server (userland).
 *
 * Note: not runnable until syscall boundary and userland loader exist.
 */

int main(void)
{
    /* TODO: receive requests, register services, return ports */
    for (;;)
        ;
    return 0;
}
